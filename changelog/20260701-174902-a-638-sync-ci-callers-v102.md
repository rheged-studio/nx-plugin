---
title: "Sync reusable CI callers to shared-workflows v1.0.2"
release_note:
created_at: "2026-07-01T17:49:02Z"
merged_at:
branch: "a-638-sync-ci-reusable-callers-shared-workflows-v102"
pr:
commit:
merge_strategy:
author: "rob@acmeskunkworks.io"
co_authors: []
category: chore
breaking: false
issues: ["A-638"]
stats:
  files_changed:
  loc_added:
  loc_removed:
  commits:
---

## Changed

- Bumped the two CI reusable-workflow callers in `.github/workflows/ci.yml`
  (`reusable-lint.yml` and `reusable-build-test.yml`) from the untagged pre-v1.0.0
  pin `9b7e7dc` to shared-workflows **v1.0.2**
  (`9febdb14373f383e3ae4f5a6e3f6bb75a59d0c3d`). Both pins move together so
  Dependabot bumps them in lockstep ([A-446](https://linear.app/rheged-studio/issue/A-446)). The caller inputs are unchanged — the
  reusable contract still matches (`eslint-args: src infrastructure/scripts`,
  `changelog-script: validate:changelog`, `typecheck: false`, `bats: true`, explicit
  shellcheck paths) — and the `GO/NO GO` gate is unaffected.
